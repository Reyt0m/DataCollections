/**
 * repeatFinished directive
 */
NetCommonsApp.directive('repeatFinished', function($timeout) {
  'use strict';
  return function(scope, element, attrs) {
    if (scope.$last) {
      $timeout(function() {
        scope.$emit('repeatFinished');
      });
    }
  };
});
